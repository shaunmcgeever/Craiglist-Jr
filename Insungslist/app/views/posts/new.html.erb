<h1>New <%= @category.name %> Post</h1>
<%= form_for [@category, @post] do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %> <br>
  <%= f.label :body %>
  <%= f.text_area :body %> <br>
  <%= f.hidden_field :user_id, value: @user.id %>
  <%= f.submit %>
<%end%>
